import { LanguageProvider } from '@/components/LanguageContext';
import ArrowFireLanding from '@/components/ArrowFireLanding';

const Index = () => {
  return (
    <LanguageProvider>
      <ArrowFireLanding />
    </LanguageProvider>
  );
};

export default Index;
